<section class="hero">
  <div class="hero-content">
    <div class="img1 big">
      <a href="">
        <img src="https://images.pexels.com/photos/2679323/pexels-photo-2679323.jpeg" alt="Hombres">
        <div class="textbanner">
          <h1>Nuestras Tiendas</h1>
        </div>
      </a>
    </div>
    <div class="generos">
      <div class="img1 small">
        <a href="">
          <img src="../herofts/ft1.jpeg" alt="Hombres"> 
            <h1>Hombres</h1>
        </a>
      </div>
      <div class="img1 small">
        <a href="">
          <img src="../herofts/ft3.jpeg" alt="Hombres">
          <h1>Mujeres</h1>
        </a>
    </div>
    </div>
  </div>
</section>

<style>
.hero {
  width: 100%;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  background: #f5f5f5;
  margin-top: -40px;
}

.hero-content {
  display: flex;
  width: 90%;
  max-width: 1400px;
  height: 80%;
  gap: 20px;
}

/* Imagen grande */
.img1.big {
  flex:2;
  position: relative;
  overflow: hidden;
  border-radius: 15px;
  animation: sladeInX 0.8s ease-out;
}

.img1.small {
  flex:1;
  position: relative;
  overflow: hidden;
  border-radius: 15px;
  animation: sladeInY 0.8s ease-out;
}

/* Imagen */
.img1 img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.5s ease;
}

/* Hover efecto */
.img1:hover img {
  transform: scale(1.05);
  cursor: pointer;
}
.generos {
  display: flex;
  gap: 20px;
  flex: 2;
}

/* Texto */
.img1 h1 {
  position: absolute;
  bottom: 15px;
  left: 15px;
  color: #fff;
  font-size: 35px;
  text-shadow: 0 4px 8px rgba(0,0,0,0.6);
}

/* Responsive */
@media (max-width: 900px) {
  .hero {
    height: auto;
    padding: 60px 0;
  }
  .hero-content {
    flex-direction: column;
    height: auto;
  }
  .img1.big, .img1.small {
    flex: 1;
    height: auto;
  }
  .img1.big{
    height: 300px;
  }
  .img1 h1 {
    font-size: 1.5rem;
  }
  .generos {
  display: flex;
  flex-direction: column;
  gap: 20px;
  flex:1;
}
}
@keyframes sladeInY {
  from {
    opacity: 0; 
    transform: translateY(30px);
  
  }
  to { 
    opacity: 1; 
    transform: translateY(0);
  }
}
@keyframes sladeInX {
  from {
    opacity: 0; 
    transform: translateX(30px);
  
  }
  to { 
    opacity: 1; 
    transform: translateX(0);
  }
}
</style>
